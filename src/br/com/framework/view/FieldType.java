package br.com.framework.view;

//Criar Enum (Text, TextArea, Relation (Combobox), Combobox boolean, data, Secret, Number (Int/Real))
public enum FieldType {
	TEXT,
	TEXT_LARGE,
	RELATION,
	BOOLEAN,
	DATE,
	SECRET,
	REAL,
	INTEGER
}
